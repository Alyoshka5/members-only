extends ../layout 

block content
    div
        a(href=currentUser.url + '/update') Update Account 

    div
        h3 Are you sure you want to delete your account?
        form(method="post") 
            button(type="submit") Delete Account

    div
        a(href=currentUser.url) Go Back
        